# Template do .env — copie para `.env` e preencha.
SERVER_IP=

POSTGRES_USER=evolution
POSTGRES_PASSWORD=             # openssl rand -hex 24

EVOLUTION_API_KEY=            # mesma chave do dashboard (env EVOLUTION_API_KEY no Vercel)
